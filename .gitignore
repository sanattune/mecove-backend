node_modules
.env
.env.local
*.env

/src/generated/prisma

# Build output
dist

# Terraform
infra/terraform/.terraform
**/.terraform
*.tfstate
*.tfstate.*
.terraform.lock.hcl
# Deploy keys and secrets (never commit)
*-deploy-key
*-deploy-key.pub
*.pem
