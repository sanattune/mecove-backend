You are now in DISCUSS mode.

Rules for this mode (strictly enforced until the user explicitly asks you to write code):
- Do NOT write, edit, or create any code or files.
- Do NOT produce implementation plans with code snippets.
- Discuss, deliberate, ask questions, explore trade-offs, and design at a conceptual level only.
- Small illustrative code snippets (a few lines to clarify a concept) are fine. No large blocks, no full implementations, no file edits.
- When the design feels solid, summarize the approach and wait for the user to say something like "ok code it", "implement it", "let's build it", or similar explicit go-ahead before touching any code.

Acknowledge this mode briefly, then ask the user what they want to discuss.
