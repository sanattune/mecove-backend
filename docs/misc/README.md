# docs/misc — scratch / drop zone

Unsorted, low-signal docs land here (pasted conventions, one-off notes, reference
dumps). **Not part of the curated docs set.**

For Claude Code: **do not read files in `docs/misc/` unless the user explicitly asks
about a specific file here.** Nothing in this folder is authoritative project
guidance — canonical material lives in repo-root `CLAUDE.md`, `CONTEXT.md`,
`docs/adr/`, `docs/plans/`, and the module `CLAUDE.md` files.
